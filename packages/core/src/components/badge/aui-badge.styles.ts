export const styles = `
/* Badge component styles */
:host {
  display: inline-block;
  position: relative;
}
.badge {
  position: absolute;
  top: -0.5em;
  right: -0.5em;
  background: var(--aui-primary);
  color: #fff;
  border-radius: 9999px;
  padding: 0.2em 0.5em;
  font-size: 0.75rem;
  line-height: 1;
}`;
