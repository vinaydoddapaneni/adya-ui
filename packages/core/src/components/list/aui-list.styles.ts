import { css } from '../../base/utils';

export const styles = css`
  :host {
    display: block;
    padding: var(--aui-spacing-sm) 0;
  }

  .aui-list {
    display: block;
    margin: 0;
    padding: 0;
    list-style: none;
    background-color: var(--aui-background-paper);
    color: var(--aui-text-primary);
  }
`;
